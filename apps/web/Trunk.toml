[build]
target = "index.html"
public_url = "/"
# Use release profile for smaller WASM builds
cargo_profile = "release"
# Minify assets in release mode
minify = "never"

[[build.targets]]
target_type = "wasm"
crate_type = "cdylib"
